{# templates/product/add.html.twig #}

{% extends 'base.html.twig' %}
{% block body %}

    {# Import the macros #}
    {% import '/cmacros.html.twig' as productMacros %}

    {# Use the macro to render the product form #}
    {{ productMacros.product_form(form) }}
{% endblock %}
